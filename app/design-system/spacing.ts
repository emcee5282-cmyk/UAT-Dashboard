// Standard <main> content padding — confirmed byte-identical across every
// page in the app (both Design System v2 and legacy-style pages) before
// this extraction.
export const PAGE_MAIN_PADDING_CLASS = 'flex-1 flex flex-col overflow-hidden px-6 pt-8 pb-6';

// KPI summary-card container — confirmed byte-identical between Balance's
// two implementations (Cashout + Send Money) before this extraction.
export const KPI_CARD_CLASS =
  'rounded-xl border border-border bg-white dark:bg-[#2a2a2d] shadow-sm flex-1 min-w-[100px] p-2.5';
